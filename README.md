# Web-Scraping
This project automates the extraction of structured data from Wikipedia pages using Python. Built inside a Jupyter Notebook environment, it parses HTML table structures, cleans textual artifacts, and converts unstructured web data into structured **Pandas DataFrames** exported as CSV files for analysis.
